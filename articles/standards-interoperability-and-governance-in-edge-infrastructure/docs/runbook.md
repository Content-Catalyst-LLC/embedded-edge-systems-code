# Runbook

From this article directory:

```bash
python python/interoperability_risk_scoring.py
Rscript r/interoperability_governance_reporting.R
```

Optional systems examples:

```bash
cd c && make && ./conformance_watchdog
cd ../cpp && make && ./semantic_gateway_profile
cd ../rust && cargo run
cd ../go && go run telemetry_gateway_governance.go
```
