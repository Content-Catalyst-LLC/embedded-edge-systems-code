# Bash Workflow Scripts

This folder contains Bash scripts for running and validating the companion workflows for:

**Standards, Interoperability, and Governance in Edge Infrastructure**

The scripts are intentionally lightweight and portable. They provide a predictable local interface for running analysis, validating manifests, generating outputs, and cleaning generated files.
