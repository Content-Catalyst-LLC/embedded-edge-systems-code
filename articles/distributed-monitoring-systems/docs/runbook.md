# Runbook

From this article directory:

```bash
bash bash/validate_manifests.sh
bash bash/run_workflows.sh
bash bash/generate_outputs.sh
```

Optional systems examples:

```bash
cd c && make && ./monitoring_node_status
cd ../cpp && make && ./distributed_monitoring_state_machine
cd ../rust && cargo run
cd ../go && go run monitoring_event_router.go
```
