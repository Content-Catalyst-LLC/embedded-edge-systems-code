# Runbook

```bash
bash bash/validate_manifests.sh
bash bash/run_workflows.sh
bash bash/generate_outputs.sh
```

Optional systems examples:

```bash
cd c && make && ./environmental_node_health
cd ../cpp && make && ./network_node_state_machine
cd ../rust && cargo run
cd ../go && go run gateway_health_aggregator.go
```
