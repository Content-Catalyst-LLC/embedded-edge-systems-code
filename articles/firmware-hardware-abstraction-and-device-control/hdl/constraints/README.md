# HDL Constraints

Add board-specific constraints for clock, reset, interrupt inputs, fault signals, ownership-valid signals, and peripheral-enable outputs.

Firmware-oriented HDL examples may include:

- interrupt-event counters
- fault-event counters
- peripheral-control gates
- timestamp counters
- register-access monitors
- hardware-assisted trace blocks
