# Runbook

```bash
bash bash/validate_manifests.sh
bash bash/run_workflows.sh
bash bash/generate_outputs.sh
```

Optional systems examples:

```bash
cd c && make && ./hal_driver_contract
cd ../cpp && make && ./device_lifecycle_state_machine
cd ../rust && cargo run
cd ../go && go run firmware_telemetry_aggregator.go
```
