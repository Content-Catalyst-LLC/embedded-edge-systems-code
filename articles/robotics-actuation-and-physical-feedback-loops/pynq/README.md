# PYNQ Companion Code

This folder contains PYNQ-oriented scaffolding for low-latency robotics stream preprocessing.

The example can run without a PYNQ board and can be extended to validate real overlays for encoder preprocessing, trajectory filtering, vibration features, or safety-signal gating.
