# PYNQ Companion Code

This folder contains PYNQ-oriented scaffolding for low-latency stream preprocessing, feature-window validation, event triggers, and telemetry framing.

The example can run without PYNQ hardware and can be extended to validate real overlays for sensor stream preprocessing and FPGA-assisted feature extraction.
