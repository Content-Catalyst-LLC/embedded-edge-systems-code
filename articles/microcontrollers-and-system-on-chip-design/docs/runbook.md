# Runbook

```bash
bash bash/validate_manifests.sh
bash bash/run_workflows.sh
bash bash/generate_outputs.sh
```

Optional systems examples:

```bash
cd c && make && ./platform_capability_contract
cd ../cpp && make && ./silicon_fit_evaluator
cd ../rust && cargo run
cd ../go && go run platform_portfolio_aggregator.go
```
