# PYNQ Companion Code

This folder contains PYNQ-oriented scaffolding for gateway preprocessing, stream timestamping, and buffer-watermark validation.

The example can run without PYNQ hardware and can be extended to validate real overlays for sensor stream preprocessing, local feature extraction, timestamp capture, buffer watermark signaling, and selective uplink triggers.
