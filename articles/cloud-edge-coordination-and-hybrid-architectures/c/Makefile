CC = gcc
CFLAGS = -Wall -Wextra -O2

all: offline_authority_timer

offline_authority_timer: offline_authority_timer.c
	$(CC) $(CFLAGS) offline_authority_timer.c -o offline_authority_timer

clean:
	rm -f offline_authority_timer
