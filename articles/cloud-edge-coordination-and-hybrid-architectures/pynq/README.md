# PYNQ Companion Code

This folder contains PYNQ-oriented scaffolding for low-latency preprocessing before selective uplink.

The example can run without PYNQ hardware and can be extended to validate real overlays for stream timestamping, feature extraction, buffer watermark signals, and selective uplink triggers.
