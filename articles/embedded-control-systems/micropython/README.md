# MicroPython Companion Code

This folder contains a microcontroller-oriented embedded-control scaffold.

The example reads a mock sensor, estimates speed, computes a proportional control command, applies a safety filter, and publishes control telemetry. It can be adapted for ESP32, RP2040, STM32, Pyboard, or other MicroPython-capable boards.
