# PYNQ Companion Code

This folder contains PYNQ-oriented scaffolding for low-latency embedded-control preprocessing.

The example can run without PYNQ hardware and can be extended to validate real overlays for encoder preprocessing, low-latency filtering, PWM preprocessing, or safety-signal handling.
