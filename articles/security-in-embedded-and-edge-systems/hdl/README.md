# HDL Companion Code

This folder contains HDL scaffolding for security-oriented stream gating.

The Verilog and VHDL examples represent a simple stream gate that forwards data only when a trust signal is valid.
