# PYNQ Companion Code

This folder contains PYNQ-oriented scaffolding for low-latency CPS signal preprocessing.

The example can run without PYNQ hardware and can be extended to validate real overlays for ADC stream preprocessing, encoder decoding, PWM command validation, safety gating, and timestamp capture.
