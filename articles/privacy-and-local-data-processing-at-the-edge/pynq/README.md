# PYNQ Companion Code

This folder contains PYNQ-oriented metadata and validation scaffolding for accelerated local filtering.

The example is hardware-safe and can run without a PYNQ board. On real hardware, it can be extended to validate and load an overlay that performs local feature extraction, redaction, or event-only transformation before uplink.
