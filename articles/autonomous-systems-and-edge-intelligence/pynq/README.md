# PYNQ Companion Code

This folder contains PYNQ-oriented scaffolding for low-latency sensor-stream preprocessing in autonomous edge systems.

The example can run without PYNQ hardware and can be extended to validate real overlays for obstacle preprocessing, stream gating, confidence feature extraction, or safety-signal handling.
