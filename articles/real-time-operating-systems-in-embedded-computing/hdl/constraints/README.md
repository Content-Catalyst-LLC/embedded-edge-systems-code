# HDL Constraints

Add board-specific constraints for clock, reset, interrupt inputs, task-active flags, queue event signals, and timing trace outputs.

RTOS-oriented HDL examples may include:

- deadline monitors
- interrupt counters
- queue-depth counters
- timestamp counters
- hardware-assisted trace capture
- watchdog progress monitors
