# Constraints

Place board-specific constraint files here when adapting the HDL examples to a real FPGA or PYNQ target.

Examples may include:

- Xilinx  files
- clock definitions
- pin assignments
- timing constraints
- interface constraints
- board metadata

For article-level companion code, this folder documents where hardware implementation constraints would live without assuming a specific FPGA board.
