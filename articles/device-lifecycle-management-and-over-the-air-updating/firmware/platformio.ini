; Firmware-style scaffold for lifecycle and OTA update examples.
; Adapt this to ESP32, RP2040, STM32, Arduino, or other target platforms.

[env:native]
platform = native
build_flags = -D OTA_LIFECYCLE_EXAMPLE
