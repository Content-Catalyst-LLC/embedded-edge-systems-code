# Constraints

Place board-specific constraints here when adapting the HDL examples to real FPGA or PYNQ hardware.

Examples include:

- Xilinx `.xdc` pin assignments
- stream input/output pins
- timestamp clock constraints
- inference trigger pins
- telemetry stream pins
- timing constraints for feature-window pipelines
