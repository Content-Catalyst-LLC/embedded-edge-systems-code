# HDL Companion Code

This folder contains HDL scaffolding for edge AI hardware/software co-design.

The examples include:

- `stream_timestamper` for acquisition-time preservation
- `feature_window_counter` for bounded window formation
- `inference_trigger` for feature-ready signaling
- `telemetry_framer` for version-aware inference telemetry
