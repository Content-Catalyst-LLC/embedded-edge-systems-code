# PYNQ Companion Code

This folder contains PYNQ-oriented scaffolding for low-latency feature preprocessing, stream timestamping, inference trigger validation, and backend parity checks.

The example can run without PYNQ hardware and can be extended to validate real overlays for feature extraction, sensor stream windows, and FPGA-assisted preprocessing.
